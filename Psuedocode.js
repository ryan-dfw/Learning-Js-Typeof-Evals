// begin here the code

// let there be an immutable array of values such as:
// "I am a string"
// false
// null
// undefined
// 0
// ""

// let there be an immutable array of tuples such as:
// 50, 51, 100
// 99 ,-2, 0
// 0, 101, 100
// 500, -500, 0

// let there be an immutable pair of pairs such as:
// -1000, 0
// -5, 0

// let there be an immutable array of arrays of some types such as:
// "cat", "cat", 6, "6",
// "five", 5, "dog", "dawg",
// 0, false, "horse", "horse",
// "eight", "eight", "ate", "ate",
// 11, "eleven", "four", "for",
// "cake", "cake", "pie", "pie",

// let there be an immutable function considering values that shall:
// on values call methods
// map a message which if the boolean representation of value
// is true, is the inverse of the inverse of its boolean state
// otherwise a string calling it falsy
// for each index in the array
// passing the message, write to standard output the message

// let there be an immutable function considering two numbers and a target that shall:
// considering if the boolean representation of if the numbers added together are greater than the target
// is true, return greater than
// otherwise, considering if the boolean representation of if the numbers added together are exactly 0
// return equal to
// otherwise, return less than

// let there be an immutable function considering two numbers that shall:
// considering if the boolean representation of if they, added together, are less than zero
// is true, return a string calling them a negative number
// otherwise, considering if the boolean representation of if they, added together, are identical to literal integer 0
// is true, return a string that says 0
// otherwise, return a string calling them a positive number

// let there be an immutable function considering the array of arrays that shall:
// call the map method on each pair
// printing to standard output the boolean result of considering whether either pairs of index items 0 and 1 or 2 and 3 are identical matches

// print to system output a header for IS TRUTHY
// evaluate the truth of the truth array

// print to system output a header for NUMBER LINE

// for each tuple of two numbers and their target,
// print to system output a message containing
// the result of their addition
// the result of evaluating the relationship of their total to the target
// and the target

// for each pair of two numbers,
// print to system output a message containing
// the result of their addition
// and the result of evaluating their sign

// print to system output a header for GREATER THAN FIVE
// consider an array of pairs such as:
// (5,6),(10,11),(0,0),(1000,-1000),(6,4),(5,5)
// for each pair in the array,
// print to system output the boolean result of evaluating if each pair's elements are both greater than 5

// print to system output a header for PAIR AND COMPARE
// evaluate the pairs in that array using that function

// halt
